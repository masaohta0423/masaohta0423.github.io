# Project thumbnails

Place project thumbnail images in this directory, then set each `thumbnail` value in `papers/papers.js`.

Recommended filenames:

- `body-sublimation.jpg`
- `roombot.jpg`

A 4:3 landscape image around 1200 × 900 px is recommended. Images are cropped with `object-fit: cover`.
